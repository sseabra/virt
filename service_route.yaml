- name: service create
    kubernetes.core.k8s:
      src: service.yaml
      namespace: arrowvirt
      state: present

- name: route create
    kubernetes.core.k8s:
      src: route.yaml
      namespace: arrowvirt
      state: present
